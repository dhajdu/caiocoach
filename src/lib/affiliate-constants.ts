// Edge-safe constants for affiliate cookie handling.
// Imported by proxy.ts (Edge runtime) — do NOT add server-only imports here.

export const AFF_REF_COOKIE = 'aff_ref';
export const AFF_VISITOR_COOKIE = 'aff_visitor';
export const AFF_COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 60; // 60 days
